import { describe, expect, it } from 'vitest';

describe('add', () => {
  it('adds two numbers', () => {
    expect(1 + 2).toBe(3);
  });
});

describe('multiply', () => {
  it('multiplies two numbers', () => {
    expect(2 * 3).toBe(6);
  });
});
